
{% extends 'base.html.twig' %}

{% block title %}Inscription
{% endblock %}

{% block body %}
	<section class="container my-5 text-start  ">
		<div class="row justify-content-center my-5">
			<div class="bg-warning-subtle col-sm-10 col-md-8 col-lg-6 col-xl-6 my-3 bg-light">
				<h1 class="text-center">Inscription</h1>

				{{ form_errors(registrationForm) }}
				{{ form_start(registrationForm) }}
                    <fieldset>
				{{ form_row(registrationForm.lastname) }}
				{{ form_row(registrationForm.firstname) }}
				{{ form_row(registrationForm.pseudo) }}
				{{ form_row(registrationForm.email) }}
                    </fieldset>

				{{ form_row(registrationForm.plainPassword,
                 {label: 'Mot de passe' }) }}

				{{ form_row(registrationForm.agreeTerms) }}
            <div class="text-center">
                <button type="submit" class="btn btn-dark btn-lg mb-3">
                     M'inscrire</button>
                    <a href="{{ path('app_login') }}" class="btn btn-secondary btn-lg mb-3">Me connecter</a>
            </div>
				{{ form_end(registrationForm) }}
			</div>
		</div>
	</section>

{% endblock %}



























{# {% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
<div class="container m-5 p-5">
    <h1>S'enregistrer</h1>

    {{ form_errors(registrationForm) }}

    {{ form_start(registrationForm) }}

        {{ form_row(registrationForm.firstname) }}
        {{ form_row(registrationForm.lastname) }}
        
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn btn-primary">Envoyer</button>

    {{ form_end(registrationForm) }}
</div>
{% endblock %} #}
